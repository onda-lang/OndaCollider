buffers:
  waveform

params:
  freq = 220.0 {20.0, 2000.0}

init:
  phase = 0.0

sample:
  frames = f32(waveform.len())
  out1 = waveform.readLW(phase)
  phase = wrap(phase + freq * frames / SR, 0.0, frames)
